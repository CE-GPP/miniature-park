{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "运行脚本: test",
            "request": "launch",
            "command": "npm run test",
            "cwd": "${workspaceFolder}"
        }
    ]
}